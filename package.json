{
  "name": "lottery-algorithm",
  "version": "1.0.0",
  "description": "An academic algorithm exercise using NodeJS, Typescript and a lot of creativity.",
  "main": "index.js",
  "scripts": {
    "start:dev": "nodemon",
    "start": "npm run build && node build/index.js",
    "build": "rimraf ./build && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "lottery",
    "algorithm",
    "js",
    "typescript"
  ],
  "author": "Felipe Lautenschlager",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^17.0.36",
    "nodemon": "^2.0.16",
    "rimraf": "^3.0.2",
    "ts-node": "^10.8.0",
    "typescript": "^4.7.2"
  },
  "dependencies": {
    "rxjs": "^7.5.5"
  }
}
