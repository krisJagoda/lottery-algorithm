{
  "name": "lottery-algorithm",
  "version": "1.0.0",
  "description": "An academic algorithm exercise using NodeJS, Typescript and a lot of creativity.",
  "main": "index.js",
  "scripts": {
    "start:dev": "nodemon",
    "start": "npm run build && node build/index.js",
    "build": "rimraf ./build && tsc",
    "build:prod": "npm run test && rimraf ./build && tsc",
    "test": "jest --collect-coverage"
  },
  "jest": {
    "transform": {
        "^.+\\.(ts|tsx)$": "ts-jest"
    }
  },
  "keywords": [
    "lottery",
    "algorithm",
    "js",
    "ts",
    "typescript"
  ],
  "author": "Felipe Lautenschlager",
  "license": "ISC",
  "devDependencies": {
    "@jest/globals": "^29.2.0",
    "@types/jest": "^29.1.2",
    "@types/node": "^17.0.36",
    "nodemon": "^2.0.16",
    "rimraf": "^3.0.2",
    "ts-jest": "^29.0.3",
    "ts-node": "^10.8.0",
    "typescript": "^4.7.2"
  },
  "dependencies": {
    "@types/mocha": "^10.0.0",
    "jest": "^29.2.0",
    "mocha": "^10.0.0",
    "rxjs": "^7.5.5"
  }
}
