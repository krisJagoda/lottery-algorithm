import {UserInput} from "../../utils/user-input";

describe('User Input', () => {

    it('should return numeric value when user prompted to enter the upperBoundary value',  () => {
        //placeholder
    });

});